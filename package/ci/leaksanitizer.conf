# Configuration file for LeakSanitizer run on the Travis CI

# Qt4 leaks stuff from some internal QString functions (8 allocations, 476
# bytes every time)
leak:libQtCore.so

# Direct leak of 7280 byte(s) in 1 object(s). Happens only on old Assimp from
# Travis CI, not reproducible on my machine
leak:libassimp.so
